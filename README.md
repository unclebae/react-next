# Unclebae React - Next Tutorials

## Install & Setup

### install

```
$ npm init
$ npm install next react react-dom
```

### setup

open package.json

```
{
    "scripts": {
        "dev": "next",
        "build": "next build",
        "start": "next start"
    }
}
```

### make a directory that name is pages.

mkdir pages